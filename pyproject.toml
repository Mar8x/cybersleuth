[project]
name = "cybersleuth"
version = "0.4.0"
description = "OSINT & infrastructure analysis MCP server for Claude"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "requests",
    "mmh3",
    "python-whois",
    "dnspython",
    "shodan",
    "beautifulsoup4",
    "urllib3",
]

[project.optional-dependencies]
test = ["pytest>=8.0", "pytest-mock>=3.14"]

[project.scripts]
cybersleuth = "server:mcp.run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["server.py", "tools.py"]
